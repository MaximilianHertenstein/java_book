[[questions]]
type = "MultipleChoice"
prompt.prompt = "Was bewirkt `Utils.doWithOneToThree(Utils::showNumber)`?"
prompt.distractors = [
  "Es wird nur einmal `showNumber(1)` aufgerufen.",
  "Die Methode `doWithOneToThree` wird als Wert gespeichert.",
  "Der Code erzeugt einen Fehler, weil die Klammern bei `showNumber` fehlen."
]
answer.answer = "`showNumber` wird nacheinander mit `1`, `2` und `3` aufgerufen."
context = "`doWithOneToThree` ruft die übergebene Methode mit `accept(1)`, `accept(2)` und `accept(3)` auf."
id = "bd13a1c8-70c4-4b6f-b35f-017994009cf5"
