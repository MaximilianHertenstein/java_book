# Syntaxfehler


In großen Programmen kann es schnell schwierig werden, Syntaxfehler zu
finden.

```java, java-exec
int doppelt(int x) {
    return 2 * x;
}
int quadrat(int x)
    return x * x;
```

Deshalb zeigt die Umgebung in der Fehlermeldung genau an, in welcher Zeile der Fehler aufgetreten ist und welches Zeichen dort erwartet wurde.

Darunter wird angezeigt, an welcher Stelle welches Zeichen erwartet wurde.

## Aufgaben

[Zu den Aufgaben zu diesem Kapitel](./syntaxfehler_aufgaben.md)
